services:
  db:
    image: pgvector/pgvector:pg17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: appothecary
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: appothecary
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
